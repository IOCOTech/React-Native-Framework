import {
  theme,
  spacing,
  font_size,
  font_discription,
  font_line_height,
  font_weight,
  font_family,
  icon_size,
  border_thickness,
  radius_size
} from "./global";
import {
  image_urls,
} from "./images";

const G = {
  theme,
  spacing,
  font_size,
  font_discription,
  font_line_height,
  font_weight,
  font_family,
  icon_size,
  border_thickness,
  radius_size,
  image_urls
};

export default G;
