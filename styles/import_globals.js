import {
  Palette,
  Gutter,
  TextSize,
  Font,
  FontFamilyType,
  IconSize,
  ImageUrl,
  RadiusSize,
  DeviceSize,
  IsPlatform,
  SafeArea,
} from "./global";

const G = {
  Palette,
  Gutter,
  TextSize,
  Font,
  FontFamilyType,
  IconSize,
  ImageUrl,
  RadiusSize,
  DeviceSize,
  IsPlatform,
  SafeArea,
};

export default G;
